<!DOCTYPE html>
<html>
	
	<%- include("partials/head") %> 
	  
  	<body>

		<%- include("partials/titulo") %> 

		<% if (typeof series != 'undefined') { %>
			<div class="container col">
			
				<!-- Titulo -->
				<div class="container col" style="margin-right: 5%;">
					<h1>Series</h1>
				</div>

				<div class="container col">
					<!-- Lista de todos las series -->
					<ul>
						<% series.forEach(serie => { %>
							<li><%= serie.title %></li>
						<% }); %>
					</ul>
				</div>
			</div>
		<% } %>

  	</body>
</html>