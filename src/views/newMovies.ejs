<!DOCTYPE html>
<html>
  <head>
  </head>
  <body>

    <ul>
      <% newMovies.forEach(movie => { %>
        <li><%= movie.title %></li>
        <li><%= movie.release_date %></li>
      <% }); %>
    </ul>
    
  </body>
</html>